import Chart from '@/core/chart';

export default class Pie extends Chart {
  constructor() {
    super();
  }

  draw() {
    const render = () => {
      
    };

    render();

    return render;
  }
}